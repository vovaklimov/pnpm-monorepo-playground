{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
        "build": {
            "dependsOn": ["^gen", "^build"],
            "outputs": ["dist/**", "build/**"]
        },
        "gen": {
            "outputs": ["generated/**"]
        }
    }
}
